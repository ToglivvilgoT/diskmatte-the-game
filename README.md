# diskmatte-the-game

## Type Checking

This project uses `mypy` with `django-stubs` for long-term Django type checking support.

### Install

```powershell
venv\Scripts\python -m pip install -r requirements-dev.txt
```

### Run

```powershell
venv\Scripts\python -m mypy diskmatte-the-game
```
